name: Actions Workflow

on: 
  pull_request:
    types:
      - opened
    branches:
      - 'develop'

jobs:
  run-actions:
    runs-on: ubuntu-latest
    steps:
      - name: print
        run: echo "Hellow world"

